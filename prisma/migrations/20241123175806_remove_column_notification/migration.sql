/*
  Warnings:

  - You are about to drop the column `receiveUserId` on the `notifications` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE "notifications" DROP COLUMN "receiveUserId";
